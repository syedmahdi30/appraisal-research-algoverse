% Venue toggle — the ONLY line that differs between the two submissions.
%   \shortfalse : VLM4RWD      (Aug 30, 8 main-text pages, NeurIPS conference format)
%   \shorttrue  : Interp4Discovery (Aug 29, 5 main-text pages, 6 for camera-ready)
% Both venues exclude references AND appendices from the limit, so every block
% guarded by \ifshort is RELOCATED to the appendix, never deleted.
% Do not edit by hand during a build; scripts/build-paper.sh sets it.
\shortfalse
