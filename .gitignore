# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/

# Agent worktrees and execution ledgers
.worktrees/
.superpowers/

# OS
.DS_Store

# Secrets / env
.env

# Data (large, gated — never commit)
data/raw/
data/processed/
*.zip
*.mat
*.npy
*.npz

# Results (regenerable — not tracked)
/results/

# Model / HF caches
*.safetensors
hf_cache/

# Internal strategy & session transcripts — keep local, not in submission repo
compass_artifact_*.md
context*.md
session*context*.md
graphify-out/
submission_*.zip

# Local Claude settings (personal permission allowlist, not shared)
.claude/settings.local.json

# Local staging duplicate of results — contains gated EMOTIC data (see data-rules)
/algoverse-appraisal/

# EMOTIC-derived processed tables anywhere (gated — never redistribute)
**/data/processed/
paper/build/
paper-build/

# macOS metadata
.DS_Store

# Built and superseded PDFs are regenerable
*.pdf
archive/**/*.pdf
# Generated by scripts/build-overleaf.sh from paper/ — regenerate, never hand-edit
overleaf/

