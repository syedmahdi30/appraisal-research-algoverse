# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/

# OS
.DS_Store

# Secrets / env
.env

# Data (large, gated — never commit)
data/raw/
data/processed/
*.zip
*.mat
*.npy
*.npz

# Results (regenerable — not tracked)
/results/

# Model / HF caches
*.safetensors
hf_cache/

# Internal strategy doc — keep local, not in public repo
compass_artifact_*.md

# Local Claude settings (personal permission allowlist, not shared)
.claude/settings.local.json

# Local staging duplicate of results — contains gated EMOTIC data (see data-rules)
/algoverse-appraisal/

# EMOTIC-derived processed tables anywhere (gated — never redistribute)
**/data/processed/
paper/build/
paper-build/

# macOS metadata
.DS_Store

# Superseded drafts are kept as source; their built PDFs are regenerable
archive/**/*.pdf
